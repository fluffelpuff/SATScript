package config

// Wird verwendet um eine Configurationsdatei einzulesen
func LoadOrCreateConfigFile(file_path *PathConfigs) (string, error) {
	// Es wird geprüft ob die Config Datei vorhanden ist

	return "", nil
}
